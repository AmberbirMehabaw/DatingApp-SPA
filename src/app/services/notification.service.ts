import { Injectable } from '@angular/core';
// import { NbToastrService, NbGlobalPhysicalPosition } from '@nebular/theme';

@Injectable({
  providedIn: 'root'
})
export class NotificationService {
  // public position = NbGlobalPhysicalPosition.TOP_RIGHT;
  // public limit = 3;
  // private readonly duration = 3000;
  // constructor(private toastrService: NbToastrService){}

  //   showToastr(status:string, title:string){
  //       this.toastrService.show("", title,
  //         {limit: this.limit, position: this.position, status: status, duration: this.duration, icon:''})
  //   }
}
